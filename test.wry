class Greeter do
	def init(person) do
	    var self.name = person.name
	    var self.greeting = "Hello, " + person.name +
	        "! I hear you like " + person.favoriteFood + ". That's my favorite food, too!"
	end
end

print(Greeter({ name: "Abe", favoriteFood: "ice cream" }).greeting)
